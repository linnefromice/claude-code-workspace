---
description: rules-distillスキルのレガシースラッシュエントリーシム。スキルを直接使用することを推奨します。
---

# Rules Distill（レガシーシム）

`/rules-distill` をまだ呼び出している場合のみ使用してください。メンテナンスされているワークフローは `skills/rules-distill/SKILL.md` にあります。

## 正規サーフェス

- `rules-distill` スキルを直接使用することを推奨します。
- このファイルは互換性のエントリーポイントとしてのみ保持してください。

## 引数

`$ARGUMENTS`

## 委譲

`rules-distill` スキルを適用し、ここでそのロジックを複製する代わりに、スキルのインベントリ、クロスリード、判定ワークフローに従ってください。
